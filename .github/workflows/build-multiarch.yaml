name: BuildContainerImage

on:
  push:
    branches:
      - 'master'
      - 'release-*'
      - 'kse-3.3'
    tags:
      - 'v*'

jobs:
  build:
    name: Build
    runs-on: self-hosted
    env:
      GO111MODULE: on
    steps:
      - name: Check out code into the Go module directory
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v1
        with:
          platforms: all

      - name: Set up Docker buildx
        uses: docker/setup-buildx-action@v1

      - name: Build and push docker images
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
          DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
        if: github.event_name == 'push'
        run: |
          echo ${{ secrets.DOCKER_PASSWORD }} | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin registry.cn-beijing.aliyuncs.com
          REPO=registry.cn-beijing.aliyuncs.com/qkcp TAG="${GITHUB_REF#refs/*/}" make container-cross-push
